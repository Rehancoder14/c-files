#include <iostream>
using namespace std;

int main(){
    
    const int a = 50;
    cout<<"the value of a is "<<a;
//const are the constant in  c++ which allows user not to change the variable 
// variable will not be changed
    return 0;
}